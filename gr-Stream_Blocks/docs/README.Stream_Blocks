----- INSTALLATION GUIDE -----
1. clone the folder to your PC
2. navigate to gr-Stream_Blocks folder from the terminal

run the following commands:
    mkdir build
    cd build
    cmake ../
    make
    sudo make install
    sudo ldconfig

Now the block should be available in GNU Radio

Execute the example flow graph in 'examples' folder to see how the parameters are set and verify its operation
