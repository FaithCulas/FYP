----- INSTALLATION GUIDE -----
1. clone the folder to your PC
2. navigate to gr-Stream_Blocks folder from the terminal

run the following commands:
    mkdir build
    cd build
    cmake ../
    make
    sudo make install
    sudo ldconfig

Now the blocks should be available in GNU Radio
